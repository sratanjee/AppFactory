name: scaffold
description: Deterministic app generator for App Factory. Reads a spec, materialises apps/<slug>/.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.13.0

dependencies:
  args: ^2.7.0
  path: ^1.9.1

dev_dependencies:
  test: ^1.25.15
  very_good_analysis: ^11.0.0

executables:
  scaffold: scaffold
